<%= form_for([@event, @event.chats.build]) do |f| %>

	<p>
		<%= f.hidden_field :user_name, :value => current_user.email %>
	</p>
	<p>
		<% f.label :body %><br>
		<%= f.text_area :body %><br>
	</p>
	<p>
		<%= f.submit %>
	</p>
	<% end %>